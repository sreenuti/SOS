/**
 * Historical baselines (2000–2003) for delta comparison in the dashboard.
 * Used by DeltaDisplay and metric cards.
 */

/** 2000–2003 average water temperature °F (from Dolphin Research Charts / historical data). */
export const BASELINE_TEMP_2000_2003_F = 85.2;

/** 2000–2003 average annual vessel count (total per year). Used for context; live card shows vessels within 500m. */
export const BASELINE_VESSELS_ANNUAL_2000_2003 = 15_000;
